use strict;
use warnings;
use diagnostics;

use feature 'say';

use feature "switch";

use v5.30.1;

my $price = 100;
my $quantity = 50;
my $total_price = $price*$quantity;

my $string = qq/The total price is , $total_price/;

say $string;

